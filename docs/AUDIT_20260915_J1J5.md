# 灵克重构 J1–J5 审计报告（2026-09-15）

> 判据依据：`docs/LINGYUAN_IRON_LAW.md`（灵元哲学工程铁律，2026-09-15 定稿）
> 核验方式：git 近 5 commit 逐一取证 + 注册表/后端实机热拔插测试 + 15 模块存储直连扫描
> 审计者：灵克（AtomCode / glm5.3-flash）
> HEAD：`faa73eb`（工作区含未提交插片级改动，不触碰主干）

---

## 总判定

**J1/J2 达标，J3 基本成立，J4/J5 欠账。**
重构方向正确、主干纪律良好；欠账集中在「存量收编」（J4）与「守卫换代」（J5），
均为可排期的工程活，不构成架构方向问题。

---

## J1 变化走接缝 —— ✅ 达标

| 证据 | 结论 |
|------|------|
| 近 5 commit（`faa73eb` GLM 默认模型 / `8eb3219` 会话 _default 回归修复 / `f37d5ad` ask 放行+mode 持久化 / `9d848c3` 全屏 TUI / `d46143b` git remote 动态读取）改动全部落在插片层（session / permissions / bash / TUI / task_router），未触碰 seam / state_store / wiring 主干 | 主干零 diff 纪律成立 |
| 工作区未提交改动（permissions / coding / session / task_router / repl / interface / full_tui）同样不含主干文件 | 当前活跃工作未违例 |
| 遗留违例：`engine/tool_handlers/bash_tools.py` handler 返回裸 `dict{"error":...}`，未走 ToolResult 协议 | 插片边界契约不统一（老欠账，非新增违例） |

## J2 可拔插可证 —— ✅ 达标（实机测试通过）

| 抽查 | 实测 |
|------|------|
| J2-1 SeamRegistry 热拔插 | register → get → unregister → 幂等 unregister（重复 no-op）全链 OK |
| J2-2 SandboxProvider 双后端 | BwrapSandboxProvider / NoopSandboxProvider 可互换，bash 内核不感知后端差异 |
| J2-3 StateStore 后端注入 | JsonFileBackend save/load round-trip OK（协议可换） |

## J3 分形成立 —— ◐ 大体成立，停层声明缺失

- ✅ bash 工具插件 manifest 完整（entry / test_entry / provides 自描述），子整体合格
- ✅ `full_tui.py` docstring 明确形态边界（"零侵入 P0/P1 逃生逻辑"）；`research_crew.py` 明确内核纪律（"治理真引擎：GovernanceEngine"）
- ✗ 分形停层声明未成制度：仅新插片有，存量插片无「内核是什么 / 子插片接缝在哪 / 几个实现」的标准声明

## J4 状态归原语 —— ✗ 最大欠账（收编 1/15）

StateStore 消费点仅 5 处（query_engine / query_engine_lifecycle_mixin /
session_runtime / wiring / core.__init__）——即 **session_state 一家已收编**。

15 个状态模块直连存储痕迹扫描：

| 模块 | 直连痕迹 | 迁移优先级 |
|------|---------|-----------|
| handover.py | 4 处 | P1（最厚） |
| layered_memory.py | 3 处 | P1 |
| memory_engine.py | 3 处 | P1 |
| session.py | 3 处 | P2（部分已走 StateStore，收尾） |
| task_aggregation.py | 2 处 | P1 |
| governance_verifier.py | 2 处 | P1 |
| topic_stack.py | 2 处 | P2 |
| reasoning_chain.py | 2 处 | P2 |
| governance.py | 1 处 | P2 |
| meta_cognition.py | 1 处 | P2 |
| cognitive_rhythm.py / skill_parser.py / behavior_aware_router.py / context_cache.py | 0 处 | 先甄别死活：纯内存或死模块，死的直接下线 |

## J5 守卫同源 —— ✗ 守卫体系与铁律错位

| 守卫 | 实际校验 | 对应判据 | 评价 |
|------|---------|---------|------|
| G8 禁内联注册 / G9 工具走 handler_name | 插片解耦 | J1 | ✅ 同源 |
| G11 禁 core import plugins / G12 插件自包含 / G13 入口约束 / G14 loader 目录 | 分形边界 | J3 | ✅ 同源 |
| **G10 行数红线**（单文件 800 + core 总量 24000，core 已零余量） | 行数 | — | ✗ **与铁律 1 直接冲突**：插片正当增长会误报；应改造为「core 模块是否私连存储」的 J4 合规检查 |
| **G3 lazy-import 基线** | import 计数 | — | ✗ 数字型守卫，本周两度因正常工作回红（424→427），慢性噪音；应降级为告警或按接缝入册豁免 |

---

## 行动排序

| 优先级 | 行动 | 验收 |
|--------|------|------|
| P1 | J4 迁移排期：按直连痕迹数排序逐个收编 StateStore（handover → layered_memory → memory_engine 优先） | 模块直连痕迹清零，StateStore 消费点覆盖全部存活状态模块 |
| P1 | J5 守卫换代：G10 改为 J4 合规检查；G3 降级告警 | 守卫红灯仅对架构违例触发，正常插片增长不再误报 |
| P2 | J3 制度化：插片停层声明模板入 PluginLoader 校验（新插片注册时强制） | 新插片无停层声明即拒绝入册 |
| P2 | J1 收尾：ToolResult 协议全链路强制（bash_tools 等 handler 收尾） | G4 类检查扩展至全部 handler 边界 |

---

*报告位置：`docs/AUDIT_20260915_J1J5.md` ｜ 下轮审计对照本表核销欠账。*
