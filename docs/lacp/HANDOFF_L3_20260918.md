# 交接文档：向外扩展四层宏图 · 第三层（L3）落盘 · 2026-09-18 会话中断

> **定位**：本会话因上下文临界中断，本文是新会话的**续作入口**。
> 新会话读本文 + `docs/lacp/CAPABILITY_PLUGIN_PLAN_L3.md` + `docs/LINGYUAN_IRON_LAW.md`
> §一 铁律 5-8 即可续作，不必重放本会话。
> **记录者**：AtomCode（glm5.3-flash）；**中断时间**：2026-09-18。

---

## 一、四层权威口径（用户 2026-09-18 裁定，锁死，勿再漂移）

| 层 | 内容 | 状态 |
|----|------|------|
| **第一层** | **把现有灵族 Agent + 对外工程项目作为插片接入 lc，共 20 实体（12 子 + 8 对外工程）**，lingxi 为首片 | lingxi 已通（agent/lingxi 三件套+M5 截肢✅）；**12 子 + 8 工程批量接入交 lc**（第一层） |
| **第二层** | Coding Agent 全插件化（Claude Code/Codex/Cursor/opencode/Zcode/AtomCode → AGENT 缝插片） | 与第一层同域前缀 `agent/`，协议统一（指令→结果）；lingxi 是它的首个试验田 |
| **第三层** | **能力域插片化**（Hermes/浏览器/工作流/桌面/训推） | 方案已落盘 `CAPABILITY_PLUGIN_PLAN_L3.md`，**2 个待裁决问题挂着**（见 §三） |
| **第四层** | OS/硬件插片化（Linux/Windows/Android/iOS） | 待第三层"养"（cap/infer 是"第二层养第四层"的第一块砖，训推域插片化后 OS 资源探针才有监控锚点） |

**分级锁定（用户已选）**：12 子 T2 / 基础设施（LingBus/lingxi/zhibridge）T1 / lingan T1（安全门全审计）/ 8 对外工程 T3；拔插默认 L1、T3 工程 L3 缺席裸奔；灵依=external_project（第一层 T3）、灵通+=retired 不挂载。

**灵依/灵通+ 口径**（ling_org 已定）：灵依并入对外工程（AI 健康助手），灵通+=retired（record 保留存在证明，插片不注册）。

---

## 二、本会话已落盘产物（全部验证过，新会话可直接用）

### 代码（16 测试全绿）
| 产物 | 位置 | 说明 |
|------|------|------|
| agent/lingxi 插片 | `lingclaude/plugins/agents/agent_lingxi/`（manifest+plugin+测试） | 第一层首片，T1/L1，MCP stdio，M5 截肢✅ |
| bus_bridge | `lingclaude/plugins/agents/bus_bridge.py` + `tests/agents/test_bus_bridge.py`（6 测试） | LingBus→插片路由桥：路由表 record 化+N2/absent 守卫+J4 含 error 留痕 |
| work_claim 锁 | `lingclaude/plugins/agents/work_claim.py` + `tests/agents/test_work_claim.py`（8 测试） | bind/renew/release/holder/check_paths 五原语，超时自动失效，操作域/时效域 |
| worktree_node | `scripts/worktree_node.py` + `tests/agents/test_worktree_node.py`（8 测试） | 代码层隔离（worktree）+record 层共享，锁同生命周期，脏区拒销 |
| conftest 查锁跳过 | `tests/agents/conftest.py` | 守卫跑测试前查 work_claim，被锁模块 skipped:claim-held 显式留痕 |

### 条文/文档
| 产物 | 位置 | 说明 |
|------|------|------|
| 铁律 8 升格（三域） | `docs/LINGYUAN_IRON_LAW.md` §一 L125 | 隔离故障域+操作域+时效域，N4 扩为三查（缺席/互斥/时效） |
| mcp-wrap skill | `.atomcode/skills/mcp-wrap/SKILL.md` | MCP 封装模板（输入 7 项/四件套/踩坑 7 条），全族复用 |
| 第三层方案 | `docs/lacp/CAPABILITY_PLUGIN_PLAN_L3.md` | 能力域插片化 5 域（cap/infer、cap/crew、cap/browser、os/resource、cap/hermes） |
| 第一层方案 | `docs/lacp/AGENT_PLUGIN_INTEGRATION_PLAN.md` | 12 子 + 8 工程 → AGENT 缝集成（12 子交 lc 批量做） |
| 演进实录 | `docs/lacp/LINGYUAN_EVOLUTION_RECORD_20260917.md` | 2026-09-17 会话 11 章回放 |

### 修订史（arch_law_revision 可 query）
20260918-01（铁律 8 三域升格 promoted）/ -02（work_claim/worktree 硬化 enacted）/ -03（第一层权威定义 drafted）。

---

## 三、挂着未裁决 / 未完成的（新会话续作点）

### 3.1 第三层（L3）2 个待用户裁决问题（方案 §六）
1. **SeamType.RESOURCE 是否新增进 core/seam.py**（走铁律 1 接缝协议，M3+M5 终审）？
   还是先全用 T3 探针插片不改枚举（过渡态，后期还要迁一次）？
   **AtomCode 建议：前者**（枚举一等公民，N4/N5 守卫好挂）。
2. **Phase A（cap/infer 还债）是否开工**？依赖灵元栈 13 端口实测（MEMORY 记载 llama-server 已升级，但未探）。
   **AtomCode 建议：先跑 13 端口存活探测**（nvidia-smi/GET /v1/models 逐一），探通多少封多少，探不通记 debt。

### 3.2 第一层 ling_org 缺录（任务 #8 未完成的尾巴）
ling_org 现有 15 条 org_member（12 子 + lingyi + zhibridge 等），**8 个对外工程全部未灌录**：
- 缺：灵康/灵律/灵声/灵视/灵触/灵戴/灵商/灵依（8 条，其中灵依已在 ling_org 作为 lingyi 存在但需标 external_project）
- **新会话先补这 8 条 org_member（type=external_project，trust=T3，plug=L3）再谈第三层开工**——第一层 20 实体要齐 20 条台账记录。
- 12 子的 ling_org key 是拼音全称（lingflow/lingzhi/...），与"通克研知优信网桥道扬创安"简名是同一批，对照时别按简名查（本会话已踩此坑）。

### 3.3 未提交 git 变更（H17 提醒，新会话提交前对照）
```
 M .atomcode/memory.md
 M data/arch_ledger/arch_audit_state/default.json
 M docs/LINGYUAN_IRON_LAW.md
 M docs/lacp/LINGYUAN_EVOLUTION_RECORD_20260917.md
?? .atomcode/skills/                          (mcp-wrap)
?? data/arch_ledger/arch_audit_task/audit-work-claim-guard.json
?? data/arch_ledger/arch_law_revision/20260918-{01,02,03}.json
?? docs/lacp/AGENT_PLUGIN_INTEGRATION_PLAN.md
?? docs/lacp/CAPABILITY_PLUGIN_PLAN_L3.md
?? lingclaude/plugins/agents/                 (agent_lingxi + bus_bridge + work_claim)
?? scripts/worktree_node.py
?? tests/agents/                              (4 测试文件 + conftest)
```
另：本交接文档自身也待入。提交需用户发话（灵元纪律：git 提交须用户授权）。

### 3.4 在册待办（arch_audit_task / arch_debt，新会话可 query）
- `agent-lingxi-mcp-probe-timeout`（debt，due 09-24）：真实 MCP 探针超时，npx 冷启动疑因，node 直启已修但端点未复验；
- `audit-work-claim-guard`（P3）：work_claim 候选守卫（本会话已硬化为代码，此 task 可关）；
- `audit-candzone-*`（4 条，P0/P1）：候选区失锁返审项，已修复但 state=open，**新会话核账后关闭**；
- 候选区 5 笔裁决债（due 10-17）：候选铁律 5-8 裁决（本会话 5/6/7/8 已全升格，此 5 笔债可一并关闭）。

### 3.5 LingBus 在途线程
- `lingxi-git-mcp-proposal`（f24c687a）：git-MCP 立项建议已发 lingxi，等回报后更新 agent/lingxi capabilities；
- `lingyuan-phase1-closure`（6d939db7）：Phase 1 收口广播已发。

---

## 四、新会话开工建议顺序

1. **先补 ling_org 8 条**（§3.2，第一层 20 实体补齐，半小时）；
2. **裁决 L3 两问题**（§3.1，用户发话即可开工 Phase A cap/infer）；
3. **关闭已修复的在册 task**（§3.4：audit-candzone-* 4 条 + 候选区 5 笔裁决债 + work_claim 候选守卫）；
4. **提交 git**（§3.3，需用户授权）；
5. Phase A cap/infer 还债开工（灵元栈 13 端口探测）。

---

*本文档位置：`docs/lacp/HANDOFF_L3_20260918.md` ｜ 叙事层，事实层以 `data/arch_ledger/` + `data/ling_org/` 台账为准（H17）。*
